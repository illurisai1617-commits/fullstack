# B&W Webcam

A minimal black-and-white webcam viewer that runs in the browser.

## Run

Webcam access requires a secure context. Serve the folder locally, then open the page:

```powershell
python -m http.server 8080
```

Then visit [http://localhost:8080](http://localhost:8080).
